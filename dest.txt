#include <stdio.h>

int main()
{
    int i = 0;
    FILE *fp = NULL;

    fp = fopen("number.txt", "r");

    for(i = 0; i < 100; i++){
        int x = 0;
        fscanf(fp, "%d", &x);
        printf("%d ", x);
    }

    printf("\n");

    fclose(fp);

    return 0;
}